// make a http api call
export async function makeHtrpApiCall(inputs: any) {
  try {
    // replace this with actual code
    await new Promise((resolve) => setTimeout(resolve, 10000));

    // returns output and nextnodeID
    return { data: inputs };
  } catch (error) {
    //: todo define the error message structure
    throw new Error("Error executing makeHtrpApiCall");
  }
}
