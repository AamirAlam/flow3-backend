"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
exports.returnCoinbaseWallet = returnCoinbaseWallet;
exports.tradeCoinbase = tradeCoinbase;
exports.executeDeployAndMintNFT = executeDeployAndMintNFT;
// load coinbase cdp wallet instance
async function returnCoinbaseWallet(args) {
    try {
        // replace this with actual code
        await new Promise((resolve) => setTimeout(resolve, 10000));
        // define the fixed returned type for all nodes
        return { data: args };
    }
    catch (error) {
        //: todo define the error message structure
        throw new Error("Error executing returnCoinbaseWallet");
    }
}
// Trade using coinbase cdp wallet
async function tradeCoinbase(args) {
    try {
        // replace this with actual code
        await new Promise((resolve) => setTimeout(resolve, 10000));
        // define the fixed returned type for all nodes
        return { data: args };
    }
    catch (error) {
        throw new Error("Error executing tradeCoinbase");
    }
}
// Deploy and mint nft using coinbase wallet
async function executeDeployAndMintNFT(args) {
    try {
        // replace this with actual code
        await new Promise((resolve) => setTimeout(resolve, 10000));
        // define the fixed returned type for all nodes
        return { data: args };
    }
    catch (error) {
        throw new Error("Error executing executeDeployAndMintNFT");
    }
}
